{
  "name": "detox-swiftui-test-failure-example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "detox:ios:build": "detox build --configuration ios.sim",
    "detox:ios:test": "detox test --configuration ios.sim --loglevel trace --debug-synchronization 200 --detectOpenHandles"
  },
  "devDependencies": {
    "detox": "18.6.2",
    "jest": "^26.6.3",
    "jest-circus": "^26.6.3",
    "sanitize-filename": "^1.6.1"
  }
}
